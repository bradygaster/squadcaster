{
  "name": "squadcaster",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check activity-model.mjs && node --check github-activity.mjs && node --check global-activity.mjs && node --check squad-roster.mjs && node --check extension.mjs && node --check renderer.mjs",
    "test": "node --test test/*.test.mjs",
    "test:browser": "playwright test",
    "test:all": "npm run check && npm test && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
